# Content review: value pages (step 2.1)

New text for the 37 pages at `/cursor/<slug>/`, written for review before it goes live. The source of truth is `data/cursors.json`; edit it there and rebuild.

- **Description** is the search snippet (`<meta name="description">`, 140–155 characters, count in brackets).
- **Common mistakes** show the Cursor Lint rule they link to, when one applies.
- **Platform note:** 28 values reuse their existing Across systems row (now in `data/systems.json`). The 9 values without a row got a new note, shown in full.
- Unchanged text (the one-line description and the ⓘ hint) is not repeated here.

| Value | Description | Use it for | Avoid | Common mistakes | Platform note | Related |
|---|---|---|---|---|---|---|
| `auto` | cursor: auto lets the browser pick the cursor from context: an I-beam over text and an arrow elsewhere. See it live and copy the CSS, Tailwind or JSX. <sub>(150)</sub> | • Resetting a cursor that a parent element overrode<br>• Pages where text should show the I-beam and everything else an arrow | • Clickable elements, which need pointer | • Setting pointer on html or body, so plain text and empty space look clickable <sub>L07</sub> | Resolves to the platform’s own arrow or I-beam, so it always matches what the visitor is used to. | `default`, `text`, `pointer` |
| `default` | cursor: default shows the platform’s plain arrow, whatever is underneath. Use it for app chrome like toolbars and menus. Live demo and copy-ready CSS. <sub>(150)</sub> | • Toolbars, menus and tab bars in web apps<br>• Labels that shouldn’t look selectable | • Paragraphs people may want to select and copy | • Forcing default on all text, which hides the I-beam that shows text can be selected | The standard arrow on every desktop system. Its exact shape follows the OS and, on Linux, the cursor theme. | `auto`, `pointer`, `text` |
| `none` | cursor: none hides the cursor completely. Use it for games, video playback and custom cursors, and always draw a replacement. Live demo and CSS to copy. <sub>(152)</sub> | • Custom cursors drawn with HTML or canvas<br>• Video players while playback is idle<br>• Full-screen games and kiosks | • Hiding the pointer without drawing a replacement | • Hiding the cursor with no replacement and no way to bring it back <sub>L10</sub> | Hidden on every desktop system. It comes back when the pointer leaves the element, so if you hide it on idle, bring it back yourself on movement. | `url()`, `crosshair`, `auto` |
| `context-menu` | cursor: context-menu signals that more actions wait behind a right-click. Use it on rows, files and canvas objects with a custom menu. Live demo and CSS. <sub>(153)</sub> | • File and message rows with a custom right-click menu<br>• Objects on a canvas that have extra actions | • Elements that only have the browser’s default menu | • Keeping actions only in the right-click menu, where touch and keyboard users can’t reach them | <i>Existing Across systems row, unchanged</i> | `pointer`, `help`, `default` |
| `help` | cursor: help shows that more information is available, usually in a tooltip. Use it on info icons and terms that explain themselves. Live demo and CSS. <sub>(151)</sub> | • Info icons that open a tooltip<br>• Abbreviations and terms with a definition on hover | • Links to a separate help page, which need pointer | • Showing the help on hover only, with no way to open it by keyboard or touch | Usually an arrow with a small question mark. Some systems draw only the question mark, and touch screens show nothing. | `pointer`, `context-menu`, `default` |
| `pointer` | cursor: pointer shows the hand that marks links, buttons and anything clickable. Not for plain text or disabled controls. Live demo and copy-ready CSS. <sub>(151)</sub> | • Links and buttons<br>• Clickable cards and list rows<br>• Custom controls built with role="button" | • Disabled controls, which need not-allowed<br>• Plain text and whole pages | • Leaving pointer on a disabled button <sub>L06</sub><br>• Adding pointer to a clickable <div> without a role or keyboard support <sub>L09</sub> | The pointing hand on every desktop system. cursor: hand was an old Internet Explorer name that current browsers ignore. | `default`, `not-allowed`, `grab` |
| `progress` | cursor: progress shows the app is busy in the background but still accepts input. Use it for syncs, uploads and exports. Live demo and copy-ready CSS. <sub>(150)</sub> | • Background syncs and autosave<br>• Uploads and exports that don’t block the page | • Operations that block input, which need wait | • Relying on the cursor alone to show busy state, with no spinner or message in the interface | <i>Existing Across systems row, unchanged</i> | `wait`, `default`, `pointer` |
| `wait` | cursor: wait shows the app is busy and can’t take input yet. Use it for short, blocking operations, together with a visible spinner. Live demo and CSS. <sub>(151)</sub> | • Short operations that block input<br>• A dialog that is saving and can’t be closed yet | • Background work people can keep working through, which needs progress | • Leaving wait on after the work finishes or fails<br>• Showing wait with no spinner or message, which touch users never see | <i>Existing Across systems row, unchanged</i> | `progress`, `not-allowed`, `default` |
| `cell` | cursor: cell shows that a cell or a range of cells can be selected, as in a spreadsheet. Use it in editable data grids. Live demo and copy-ready CSS. <sub>(149)</sub> | • Spreadsheet cells<br>• Editable data grids with range selection | • Read-only tables, where default or text fits better | • Using cell on header rows or controls inside the grid, which aren’t selectable cells | <i>Existing Across systems row, unchanged</i> | `crosshair`, `text`, `col-resize` |
| `crosshair` | cursor: crosshair gives precise two-dimensional selection for cropping, screenshots and plotting points on charts. See it live and copy the CSS. <sub>(144)</sub> | • Screenshot and region selection tools<br>• Image cropping<br>• Plotting points on a chart | • General clickable areas, where the thin lines are hard to read | • Typing crosshar or cross-hair, which browsers ignore <sub>L01</sub> | Thin crossing lines on every desktop system. On busy or dark backgrounds they can be hard to see. | `cell`, `move`, `none` |
| `text` | cursor: text shows the I-beam that means text can be selected or edited. Set it on the whole editable box so the padding shows it too. Live demo. <sub>(145)</sub> | • Custom editors and contenteditable areas<br>• Editable regions that aren’t native inputs | • Buttons and links, even when they contain text | • Setting it on the text only, so the box’s padding still shows an arrow | The I-beam on every desktop system. Its size and contrast follow the OS and, on Linux, the cursor theme. | `vertical-text`, `auto`, `cell` |
| `vertical-text` | cursor: vertical-text is the sideways I-beam for text in a vertical writing mode, common in Japanese, Chinese and Korean layouts. Live demo and CSS. <sub>(148)</sub> | • Text set with writing-mode: vertical-rl<br>• Vertical headings in Japanese, Chinese or Korean layouts | • Horizontal text, which needs text | • Changing the cursor but not writing-mode, so horizontal text shows a sideways I-beam | <i>Existing Across systems row, unchanged</i> | `text`, `auto` |
| `alias` | cursor: alias shows that a shortcut or link will be created instead of moving the item. Use it for pinning and bookmarking. Live demo and CSS to copy. <sub>(150)</sub> | • Pinning an item to a sidebar or favorites<br>• Dragging to create a link or shortcut | • Drags that move the item, which need grabbing or move | • Showing alias for a drag that actually moves or copies the item | <i>Existing Across systems row, unchanged</i> | `copy`, `move`, `grabbing` |
| `copy` | cursor: copy shows that something will be copied, by click or by drag. Use it for copy-to-clipboard fields and duplicating drags. Live demo and CSS. <sub>(148)</sub> | • Copy-to-clipboard fields<br>• Drags that duplicate an item instead of moving it | • Drags that move the original, which need grabbing | • Showing copy, then moving the item when it’s dropped | <i>Existing Across systems row, unchanged</i> | `alias`, `move`, `pointer` |
| `move` | cursor: move shows that an object can be moved freely, as on a design canvas or whiteboard. See it live and copy the CSS, Tailwind or JSX in one click. <sub>(151)</sub> | • Objects on design canvases and whiteboards<br>• Dialogs and panels with a draggable title bar | • Sortable lists and carousels, where grab and grabbing are clearer | • Using move for panning a whole view, where all-scroll or grab fits better | A four-way arrow on most systems. Where the OS has no native version, the browser draws its own, so it can differ between browsers. | `grab`, `grabbing`, `all-scroll` |
| `no-drop` | cursor: no-drop shows that the dragged item can’t be dropped here, like a read-only folder or the wrong file type. Live demo and copy-ready CSS. <sub>(144)</sub> | • Read-only folders during a drag<br>• Drop zones that refuse the file type | • Disabled controls that aren’t drop targets, which need not-allowed | • Relying on the cursor alone to say why the drop is refused | <i>Existing Across systems row, unchanged</i> | `not-allowed`, `copy`, `grabbing` |
| `not-allowed` | cursor: not-allowed shows that the action won’t happen. Use it on disabled controls, and always explain why nearby. Live demo and copy-ready CSS. <sub>(145)</sub> | • Disabled buttons and form controls<br>• Actions blocked until a condition is met | • Controls that look disabled but still work | • Leaving pointer on a disabled control instead <sub>L06</sub><br>• Not explaining nearby why the action is unavailable | <i>Existing Across systems row, unchanged</i> | `no-drop`, `pointer`, `wait` |
| `grab` | cursor: grab shows the open hand for things that can be dragged, before the button is pressed. Pair it with grabbing while held. Live demo and CSS. <sub>(147)</sub> | • Carousels and photo strips<br>• Map and canvas panning<br>• Sortable cards before they’re picked up | • Items that can only be clicked, not dragged | • No grabbing state while the item is held <sub>L08</sub><br>• Keeping the old -webkit-grab prefix, which current browsers don’t need <sub>L03</sub> | <i>Existing Across systems row, unchanged</i> | `grabbing`, `move`, `all-scroll` |
| `grabbing` | cursor: grabbing shows the closed hand while an item is held. Swap it in on pointerdown and back to grab on release. Live demo and copy-ready CSS. <sub>(146)</sub> | • The pressed state of carousels and sortable lists<br>• Panning a map while the button is down | • The resting state, which needs grab | • Setting it only with :active, so it’s lost if the pointer leaves the element mid-drag<br>• Keeping the old -webkit-grabbing prefix <sub>L03</sub> | <i>Existing Across systems row, unchanged</i> | `grab`, `move`, `copy` |
| `n-resize` | cursor: n-resize shows an edge that can only move up, like the top of a bottom sheet. Prefer ns-resize if it moves both ways. Live demo and CSS. <sub>(144)</sub> | • The top edge of a bottom sheet<br>• Panels docked to the bottom that only grow upward | • Edges that move both up and down, which need ns-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `ns-resize`, `s-resize`, `row-resize` |
| `s-resize` | cursor: s-resize shows an edge that can only move down, like the bottom of an expanding notes field. Prefer ns-resize if it moves both ways. <sub>(140)</sub> | • The bottom edge of an expanding text field<br>• Dropdown panels that only grow downward | • Edges that move both up and down, which need ns-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `ns-resize`, `n-resize`, `row-resize` |
| `e-resize` | cursor: e-resize shows an edge that can only move right, like the edge of a sidebar docked left. Prefer ew-resize if it moves both ways. Live demo. <sub>(147)</sub> | • The right edge of a sidebar docked to the left<br>• Columns that only grow to the right | • Edges that move both left and right, which need ew-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `ew-resize`, `w-resize`, `col-resize` |
| `w-resize` | cursor: w-resize shows an edge that can only move left, like the edge of an inspector docked right. Prefer ew-resize if it moves both ways. Live demo. <sub>(150)</sub> | • The left edge of an inspector docked to the right<br>• Panels anchored to the right side | • Edges that move both left and right, which need ew-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `ew-resize`, `e-resize`, `col-resize` |
| `ne-resize` | cursor: ne-resize shows a corner that can only move up and to the right, like a panel docked bottom-left. Prefer nesw-resize for two-way corners. <sub>(145)</sub> | • The free corner of a panel docked bottom-left<br>• Floating widgets anchored to the bottom-left corner | • Corners that move both ways, which need nesw-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `nesw-resize`, `sw-resize`, `nwse-resize` |
| `nw-resize` | cursor: nw-resize shows a corner that can only move up and to the left, like a chat widget docked bottom-right. Prefer nwse-resize for two-way corners. <sub>(151)</sub> | • The free corner of a chat widget docked bottom-right<br>• Floating panels anchored to the bottom-right corner | • Corners that move both ways, which need nwse-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `nwse-resize`, `se-resize`, `nesw-resize` |
| `se-resize` | cursor: se-resize shows a corner that can only move down and to the right, like a note pinned top-left. Prefer nwse-resize for two-way corners. <sub>(143)</sub> | • The free corner of a note pinned top-left<br>• Text areas that only grow down and to the right | • Corners that move both ways, which need nwse-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `nwse-resize`, `nw-resize`, `nesw-resize` |
| `sw-resize` | cursor: sw-resize shows a corner that can only move down and to the left, like a menu anchored top-right. Prefer nesw-resize for two-way corners. <sub>(145)</sub> | • The free corner of a menu anchored top-right<br>• Popovers that open from the top-right corner | • Corners that move both ways, which need nesw-resize | • Expecting a one-way arrow on Windows, which draws a two-headed arrow for every one-way value | <i>Existing Across systems row, unchanged</i> | `nesw-resize`, `ne-resize`, `nwse-resize` |
| `ew-resize` | cursor: ew-resize shows something that resizes left and right, like a before-and-after slider or a vertical divider. See it live and copy the CSS. <sub>(146)</sub> | • Before-and-after image sliders<br>• Vertical dividers between panes<br>• Range handles | • Table column dividers, where col-resize is clearer | • Setting it on the whole slider instead of the handle, so every part looks draggable | <i>Existing Across systems row, unchanged</i> | `col-resize`, `ns-resize`, `e-resize` |
| `ns-resize` | cursor: ns-resize shows something that resizes up and down, like a bottom panel, terminal or console. See it live and copy the CSS, Tailwind or JSX. <sub>(148)</sub> | • Bottom panels like a terminal or console<br>• Timelines docked under an editor | • Dividers between stacked panes, where row-resize is clearer | • Making the draggable edge only a pixel or two tall, so it’s hard to grab | <i>Existing Across systems row, unchanged</i> | `row-resize`, `ew-resize`, `n-resize` |
| `nesw-resize` | cursor: nesw-resize resizes diagonally from the top-right and bottom-left corners, as on crop boxes and selection frames. Live demo and CSS to copy. <sub>(148)</sub> | • The top-right and bottom-left handles of crop boxes<br>• Selection frames on a canvas | • The other corner pair, which needs nwse-resize | • Using the same diagonal cursor on all four corners | <i>Existing Across systems row, unchanged</i> | `nwse-resize`, `ne-resize`, `sw-resize` |
| `nwse-resize` | cursor: nwse-resize resizes diagonally from the top-left and bottom-right corners, as on images and windows. See it live and copy the CSS or Tailwind. <sub>(150)</sub> | • The top-left and bottom-right handles of images<br>• Window and panel corners | • The other corner pair, which needs nesw-resize | • Using the same diagonal cursor on all four corners | <i>Existing Across systems row, unchanged</i> | `nesw-resize`, `nw-resize`, `se-resize` |
| `col-resize` | cursor: col-resize shows a column divider that can be dragged sideways, as in tables and data grids. See it live and copy the CSS, Tailwind or JSX. <sub>(147)</sub> | • Column dividers in tables and data grids<br>• Resizable column headers | • Before-and-after sliders, where ew-resize reads better | • Making the grab area as thin as the visible divider line | <i>Existing Across systems row, unchanged</i> | `row-resize`, `ew-resize`, `cell` |
| `row-resize` | cursor: row-resize shows a divider between stacked panes that can be dragged up and down, like a query and its results. Live demo and CSS to copy. <sub>(146)</sub> | • Split panes stacked vertically<br>• A query editor above its results | • Panels docked to the window’s bottom edge, where ns-resize fits | • Making the grab area as thin as the visible divider line | <i>Existing Across systems row, unchanged</i> | `col-resize`, `ns-resize`, `s-resize` |
| `all-scroll` | cursor: all-scroll shows content that can be panned in any direction, like a map or a large canvas. See it live and copy the CSS, Tailwind or JSX. <sub>(146)</sub> | • Maps<br>• Large canvases and diagrams<br>• Middle-click autoscroll areas | • Objects that move on their own, which need move | • Showing all-scroll where the content only scrolls up and down | Often drawn as a four-way arrow, and on some systems it looks the same as move. Where the OS has no native version, the browser draws its own. | `grab`, `move`, `grabbing` |
| `zoom-in` | cursor: zoom-in shows that clicking magnifies an image or product photo. Swap it for zoom-out once zoomed. Live demo and copy-ready CSS or Tailwind. <sub>(148)</sub> | • Product photos<br>• Image galleries and lightboxes | • Images that open a new page, which need pointer | • Keeping zoom-in after the image is already zoomed<br>• Keeping the old -webkit-zoom-in prefix <sub>L03</sub> | <i>Existing Across systems row, unchanged</i> | `zoom-out`, `pointer`, `crosshair` |
| `zoom-out` | cursor: zoom-out shows the zoomed state: clicking returns the image to its normal size. Swap it back to zoom-in each time. Live demo and CSS. <sub>(141)</sub> | • Zoomed product photos<br>• Lightboxes in their magnified state | • Images that aren’t zoomed yet, which need zoom-in | • Not swapping back to zoom-in after zooming out<br>• Keeping the old -webkit-zoom-out prefix <sub>L03</sub> | <i>Existing Across systems row, unchanged</i> | `zoom-in`, `pointer`, `all-scroll` |
| `url()` | cursor: url() sets your own image as the cursor, with an x y hotspot and a keyword fallback. Keep it 32 × 32 px or smaller. Live demo and editor. <sub>(145)</sub> | • Drawing tools like brushes and pens<br>• Branded cursors in games and kiosks | • Replacing standard cursors people rely on, like pointer and text | • Leaving out the keyword fallback, which makes the whole declaration invalid <sub>L04</sub><br>• Forgetting the hotspot, so clicks land on the image’s top-left corner <sub>L12</sub> | <i>Existing Across systems row, unchanged</i> | `none`, `crosshair`, `pointer` |
